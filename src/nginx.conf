# Support for secure URLs
rewrite "^/roundcube/[a-zA-Z0-9]{16}/(.*)" /roundcube/$1 last;

location ^~ /roundcube/ {
    alias {GUI_ROOT_DIR}/public/tools/roundcube/public_html;

    location ~ \.php$ {
        include imscp_fastcgi.conf;
    }
}
